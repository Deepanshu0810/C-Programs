#include<stdio.h>

int main(){
    float l,b;
    printf("Enter the length:");
    scanf("%f",&l);
    printf("Enter the breadth:");
    scanf("%f",&b);
    printf("The area of the rectangle is l*b = %f unit square",l*b);
    return 0;
}